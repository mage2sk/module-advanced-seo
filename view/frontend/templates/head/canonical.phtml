<?php
/** @var \Panth\AdvancedSEO\Block\Head\Canonical $block */
/** @var \Magento\Framework\Escaper $escaper */

$canonical = $block->getCanonicalUrl();
if ($canonical !== '') {
?>
<link rel="canonical" href="<?= $escaper->escapeUrl($canonical) ?>" data-panth-seo="canonical" />
<?php
}

// OG + Twitter emission has moved to Panth_SocialMeta — rendered via its
// own head blocks registered under head.additional in that module's
// default.xml.
//
// Hreflang alternate tags have moved to Panth_Hreflang — rendered via its
// own head block registered under head.additional in that module's
// default.xml.
?>
